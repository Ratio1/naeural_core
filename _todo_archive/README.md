# _todo_archive

This directory holds finalized, fixed, or obsolete `_todo/` notes that are still worth keeping as historical reference.

Rules:
- active planning and current result docs stay in `_todo/`
- move a file here only when it is no longer an active task doc and is not the current authoritative reference
- keep filenames unchanged when archiving so prior references remain easy to trace
